# API Base URL (Optional)
# The app automatically detects dev vs production mode:
# - Development (npm run dev): Uses localhost:8000 by default
# - Production (npm run build): Uses same origin (nginx proxy)
#
# Only set this if you need a different backend URL in development
VITE_API_BASE_URL=http://localhost:8000
